aws_region   = "ap-south-1"
image_tag    = "latest"
memory_size  = 2048
timeout      = 30
architecture = "arm64"

# Production hardening (flip when ready):
# authorization_type             = "AWS_IAM"
# cors_allow_origins             = ["https://your-frontend.example.com"]
# reserved_concurrent_executions = 20

env_vars = {
  # RATE_LIMIT = "60/minute"
}
